<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Update Book</title>
  <style>
    /* General Reset */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    /* Body Style */
    body {
      font-family: Arial, sans-serif;
      background-color: #f4f4f4;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
    }

    /* Container for Form */
    .form-container {
      background-color: white;
      padding: 20px;
      border-radius: 8px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      width: 100%;
      max-width: 500px;
    }

    /* Title Style */
    h1 {
      text-align: center;
      margin-bottom: 20px;
      color: #333;
    }

    /* Form Inputs and Labels */
    label {
      font-size: 16px;
      margin-bottom: 5px;
      display: block;
      color: #555;
    }

    input, textarea {
      width: 100%;
      padding: 10px;
      margin-bottom: 20px;
      border: 1px solid #ccc;
      border-radius: 4px;
      font-size: 16px;
    }

    textarea {
      height: 150px;
      resize: vertical;
    }

    button {
      background-color: #4CAF50; /* Green */
      color: white;
      padding: 12px 20px;
      border: none;
      border-radius: 4px;
      font-size: 16px;
      cursor: pointer;
      width: 100%;
      transition: background-color 0.3s;
    }

    button:hover {
      background-color: #45a049; /* Darker Green */
    }

    /* Responsive Design */
    @media (max-width: 768px) {
      .form-container {
        padding: 15px;
      }

      input, textarea, button {
        font-size: 14px;
        padding: 8px;
      }
    }

    @media (max-width: 480px) {
      .form-container {
        padding: 10px;
      }

      input, textarea, button {
        font-size: 12px;
        padding: 6px;
      }

      h1 {
        font-size: 20px;
      }
    }
  </style>
</head>
<body>
  <div class="form-container">
    <h1>Update Book</h1>
    <form action="/update/<%= book._id %>" method="POST">
      <label for="name">Title:</label>
      <input type="text" id="name" name="title" value="<%= book.title %>" required><br><br>
      
      <label for="author">Date:</label>
      <input type="date" id="author" name="date" value="<%= book.date %>" required><br><br>
      
      <label for="description">Story:</label>
      <textarea id="description" name="story" required><%= book.story %></textarea><br><br>
      
      <button type="submit">Save Changes</button>
    </form>
  </div>
</body>
</html>
